/**
 * E2E — MongoDB 编辑器（跳过 — 无 MongoDB 测试环境）
 * SF-QA0028 batch 2
 */
import { test } from '@playwright/test'

test.describe.skip('MongoDB Editor — 跳过（无 MongoDB 测试环境）', () => {
  test('placeholder', async () => {})
})
