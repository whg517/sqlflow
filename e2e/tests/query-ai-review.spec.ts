/**
 * E2E — AI Review（跳过 — 无真实 AI provider）
 * SF-QA0028 batch 2
 */
import { test } from '@playwright/test'

test.describe.skip('AI Review — 跳过（无真实 AI provider）', () => {
  test('placeholder', async () => {})
})
