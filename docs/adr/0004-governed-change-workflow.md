# 将高风险变更纳入可审计工单流程

状态：Accepted

SQLFlow 不允许把 AI 结论直接当作执行授权。读取请求在后端权限、解析和脱敏约束下可自助执行；DDL、DML 和受支持的 NoSQL 写操作进入工单，由确定性规则与可选 AI 提供风险信息，再由审批策略和人员决定是否执行。这个流程牺牲部分即时性，换取职责分离、失败可解释、状态可恢复和完整审计证据。

后果：AI 超时必须降级而不能绕过治理；审批与执行是两个独立动作；不同数据库的事务结果必须原样记录，不能伪装成统一的全局事务语义。
