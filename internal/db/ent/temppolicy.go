// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/whg517/sqlflow/internal/db/ent/temppolicy"
)

// TempPolicy is the model entity for the TempPolicy schema.
type TempPolicy struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Sub holds the value of the "sub" field.
	Sub string `json:"sub,omitempty"`
	// Dom holds the value of the "dom" field.
	Dom string `json:"dom,omitempty"`
	// Obj holds the value of the "obj" field.
	Obj string `json:"obj,omitempty"`
	// Act holds the value of the "act" field.
	Act string `json:"act,omitempty"`
	// ExpiresAt holds the value of the "expires_at" field.
	ExpiresAt time.Time `json:"expires_at,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt    time.Time `json:"created_at,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*TempPolicy) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case temppolicy.FieldID:
			values[i] = new(sql.NullInt64)
		case temppolicy.FieldSub, temppolicy.FieldDom, temppolicy.FieldObj, temppolicy.FieldAct:
			values[i] = new(sql.NullString)
		case temppolicy.FieldExpiresAt, temppolicy.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the TempPolicy fields.
func (_m *TempPolicy) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case temppolicy.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case temppolicy.FieldSub:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field sub", values[i])
			} else if value.Valid {
				_m.Sub = value.String
			}
		case temppolicy.FieldDom:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field dom", values[i])
			} else if value.Valid {
				_m.Dom = value.String
			}
		case temppolicy.FieldObj:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field obj", values[i])
			} else if value.Valid {
				_m.Obj = value.String
			}
		case temppolicy.FieldAct:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field act", values[i])
			} else if value.Valid {
				_m.Act = value.String
			}
		case temppolicy.FieldExpiresAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field expires_at", values[i])
			} else if value.Valid {
				_m.ExpiresAt = value.Time
			}
		case temppolicy.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the TempPolicy.
// This includes values selected through modifiers, order, etc.
func (_m *TempPolicy) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// Update returns a builder for updating this TempPolicy.
// Note that you need to call TempPolicy.Unwrap() before calling this method if this TempPolicy
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *TempPolicy) Update() *TempPolicyUpdateOne {
	return NewTempPolicyClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the TempPolicy entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *TempPolicy) Unwrap() *TempPolicy {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: TempPolicy is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *TempPolicy) String() string {
	var builder strings.Builder
	builder.WriteString("TempPolicy(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("sub=")
	builder.WriteString(_m.Sub)
	builder.WriteString(", ")
	builder.WriteString("dom=")
	builder.WriteString(_m.Dom)
	builder.WriteString(", ")
	builder.WriteString("obj=")
	builder.WriteString(_m.Obj)
	builder.WriteString(", ")
	builder.WriteString("act=")
	builder.WriteString(_m.Act)
	builder.WriteString(", ")
	builder.WriteString("expires_at=")
	builder.WriteString(_m.ExpiresAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// TempPolicies is a parsable slice of TempPolicy.
type TempPolicies []*TempPolicy
