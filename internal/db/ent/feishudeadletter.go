// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/whg517/sqlflow/internal/db/ent/feishudeadletter"
)

// FeishuDeadLetter is the model entity for the FeishuDeadLetter schema.
type FeishuDeadLetter struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// WebhookID holds the value of the "webhook_id" field.
	WebhookID int64 `json:"webhook_id,omitempty"`
	// Payload holds the value of the "payload" field.
	Payload string `json:"payload,omitempty"`
	// ErrorMessage holds the value of the "error_message" field.
	ErrorMessage string `json:"error_message,omitempty"`
	// AttemptCount holds the value of the "attempt_count" field.
	AttemptCount int64 `json:"attempt_count,omitempty"`
	// LastAttemptAt holds the value of the "last_attempt_at" field.
	LastAttemptAt time.Time `json:"last_attempt_at,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt    time.Time `json:"created_at,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*FeishuDeadLetter) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case feishudeadletter.FieldID, feishudeadletter.FieldWebhookID, feishudeadletter.FieldAttemptCount:
			values[i] = new(sql.NullInt64)
		case feishudeadletter.FieldPayload, feishudeadletter.FieldErrorMessage:
			values[i] = new(sql.NullString)
		case feishudeadletter.FieldLastAttemptAt, feishudeadletter.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the FeishuDeadLetter fields.
func (_m *FeishuDeadLetter) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case feishudeadletter.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case feishudeadletter.FieldWebhookID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field webhook_id", values[i])
			} else if value.Valid {
				_m.WebhookID = value.Int64
			}
		case feishudeadletter.FieldPayload:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field payload", values[i])
			} else if value.Valid {
				_m.Payload = value.String
			}
		case feishudeadletter.FieldErrorMessage:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field error_message", values[i])
			} else if value.Valid {
				_m.ErrorMessage = value.String
			}
		case feishudeadletter.FieldAttemptCount:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field attempt_count", values[i])
			} else if value.Valid {
				_m.AttemptCount = value.Int64
			}
		case feishudeadletter.FieldLastAttemptAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field last_attempt_at", values[i])
			} else if value.Valid {
				_m.LastAttemptAt = value.Time
			}
		case feishudeadletter.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the FeishuDeadLetter.
// This includes values selected through modifiers, order, etc.
func (_m *FeishuDeadLetter) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// Update returns a builder for updating this FeishuDeadLetter.
// Note that you need to call FeishuDeadLetter.Unwrap() before calling this method if this FeishuDeadLetter
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *FeishuDeadLetter) Update() *FeishuDeadLetterUpdateOne {
	return NewFeishuDeadLetterClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the FeishuDeadLetter entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *FeishuDeadLetter) Unwrap() *FeishuDeadLetter {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: FeishuDeadLetter is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *FeishuDeadLetter) String() string {
	var builder strings.Builder
	builder.WriteString("FeishuDeadLetter(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("webhook_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.WebhookID))
	builder.WriteString(", ")
	builder.WriteString("payload=")
	builder.WriteString(_m.Payload)
	builder.WriteString(", ")
	builder.WriteString("error_message=")
	builder.WriteString(_m.ErrorMessage)
	builder.WriteString(", ")
	builder.WriteString("attempt_count=")
	builder.WriteString(fmt.Sprintf("%v", _m.AttemptCount))
	builder.WriteString(", ")
	builder.WriteString("last_attempt_at=")
	builder.WriteString(_m.LastAttemptAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// FeishuDeadLetters is a parsable slice of FeishuDeadLetter.
type FeishuDeadLetters []*FeishuDeadLetter
