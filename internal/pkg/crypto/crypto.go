package crypto

// Encrypt encrypts plaintext using AES (placeholder).
func Encrypt(plaintext, key string) (string, error) {
	return "", nil
}

// Decrypt decrypts ciphertext using AES (placeholder).
func Decrypt(ciphertext, key string) (string, error) {
	return "", nil
}
