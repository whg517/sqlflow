# SQLFlow Environment Variables

# Admin user credentials (first run only)
ADMIN_USERNAME=admin
# REQUIRED: Set a strong password for the admin user
ADMIN_PASSWORD=

# CORS allowed origins (comma-separated)
# Example: https://sqlflow.example.com,https://app.example.com
# If not set, allows all origins (development mode)
CORS_ALLOWED_ORIGINS=
